CONTIKI_PROJECT = hello-world-iotus
CONTIKI_COMM_NEW_STACK = IoTUS
IOTUS_COMPILE_MODE = DYNAMIC
IOTUS_DYNAMIC_MODE_PROFILES = ALL ENERGY_AWARE

all: $(CONTIKI_PROJECT)

CONTIKI = ../..
include $(CONTIKI)/Makefile.include
